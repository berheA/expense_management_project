export class User {

    
    constructor(public userID:number, public username:string, public firstname:string, public lastname:string,public email:string, public role:string, public password:string){}
}
